//Generated by WriteToModellibs_ts - ModelLibs.ts
import { EmployerOfficeFormikShape } from "@/interfaces/EmployerOfficeInterfaces";
import { EmployerOffice } from "@/models/EmployerOfficeModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createEmployerOffice = async (
  employerOffice: Omit<EmployerOfficeFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await EmployerOffice.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: employerOffice.name!
    },
    { transaction: t }
  );
};

export const updateEmployerOffice = async (
  employerOffice: Omit<EmployerOfficeFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<EmployerOfficeFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await EmployerOffice.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: employerOffice.name!
    },
    {
      where: { [primaryKey]: primaryKeyValue || employerOffice[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteEmployerOffices = async (
  primaryKey: keyof Omit<EmployerOfficeFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await EmployerOffice.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
