//Generated by WriteToModellibs_ts - ModelLibs.ts
import { DivisionFormikShape } from "@/interfaces/DivisionInterfaces";
import { Division } from "@/models/DivisionModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createDivision = async (
  division: Omit<DivisionFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await Division.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: division.name!
    },
    { transaction: t }
  );
};

export const updateDivision = async (
  division: Omit<DivisionFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<DivisionFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await Division.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: division.name!
    },
    {
      where: { [primaryKey]: primaryKeyValue || division[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteDivisions = async (
  primaryKey: keyof Omit<DivisionFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await Division.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
