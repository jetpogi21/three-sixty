//Generated by WriteToModellibs_ts - ModelLibs.ts
import { PlacementStatusFormikShape } from "@/interfaces/PlacementStatusInterfaces";
import { PlacementStatus } from "@/models/PlacementStatusModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createPlacementStatus = async (
  placementStatus: Omit<PlacementStatusFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await PlacementStatus.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: placementStatus.name!
    },
    { transaction: t }
  );
};

export const updatePlacementStatus = async (
  placementStatus: Omit<PlacementStatusFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<PlacementStatusFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await PlacementStatus.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: placementStatus.name!
    },
    {
      where: { [primaryKey]: primaryKeyValue || placementStatus[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deletePlacementStatus = async (
  primaryKey: keyof Omit<PlacementStatusFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await PlacementStatus.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
