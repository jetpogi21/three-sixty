//Generated by WriteToModellibs_ts - ModelLibs.ts
import { PriorityFormikShape } from "@/interfaces/PriorityInterfaces";
import { Priority } from "@/models/PriorityModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createPriority = async (
  priority: Omit<PriorityFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await Priority.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: priority.name!
    },
    { transaction: t }
  );
};

export const updatePriority = async (
  priority: Omit<PriorityFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<PriorityFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await Priority.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: priority.name!
    },
    {
      where: { [primaryKey]: primaryKeyValue || priority[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deletePriorities = async (
  primaryKey: keyof Omit<PriorityFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await Priority.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
