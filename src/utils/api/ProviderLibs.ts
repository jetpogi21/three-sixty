//Generated by WriteToModellibs_ts - ModelLibs.ts
import { ProviderFormikShape } from "@/interfaces/ProviderInterfaces";
import { Provider } from "@/models/ProviderModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createProvider = async (
  provider: Omit<ProviderFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await Provider.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      name: provider.name!,
    },
    { transaction: t }
  );
};

export const updateProvider = async (
  provider: Omit<ProviderFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<ProviderFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await Provider.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      name: provider.name!,
    },
    {
      where: { [primaryKey]: primaryKeyValue || provider[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteProviders = async (
  primaryKey: keyof Omit<ProviderFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await Provider.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
