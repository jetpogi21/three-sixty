//Generated by WriteToModellibs_ts - ModelLibs.ts
import { PositionFormikShape } from "@/interfaces/PositionInterfaces";
import { Position } from "@/models/PositionModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createPosition = async (
  position: Omit<PositionFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await Position.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: position.name!
    },
    { transaction: t }
  );
};

export const updatePosition = async (
  position: Omit<PositionFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<PositionFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await Position.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
name: position.name!
    },
    {
      where: { [primaryKey]: primaryKeyValue || position[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deletePositions = async (
  primaryKey: keyof Omit<PositionFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await Position.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
