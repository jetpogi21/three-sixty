//Generated by WriteToModellibs_ts - ModelLibs.ts
import { CandidateStatusFormikShape } from "@/interfaces/CandidateStatusInterfaces";
import { CandidateStatus } from "@/models/CandidateStatusModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createCandidateStatus = async (
  candidateStatus: Omit<CandidateStatusFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await CandidateStatus.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      candidateID: parseInt(candidateStatus.candidateID as string),
      statusID: parseInt(candidateStatus.statusID as string),
    },
    { transaction: t }
  );
};

export const updateCandidateStatus = async (
  candidateStatus: Omit<CandidateStatusFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<CandidateStatusFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await CandidateStatus.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      candidateID: parseInt(candidateStatus.candidateID as string),
      statusID: parseInt(candidateStatus.statusID as string),
    },
    {
      where: { [primaryKey]: primaryKeyValue || candidateStatus[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteCandidateStatus = async (
  primaryKey: keyof Omit<CandidateStatusFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await CandidateStatus.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
