//Generated by GetuseModelListts - useModelList.ts
"use client";
import { GetDivisionsResponse } from "@/interfaces/DivisionInterfaces";
import { DivisionSearchParams } from "@/interfaces/DivisionInterfaces";
import { BasicModel } from "@/interfaces/GeneralInterfaces";
import axiosClient from "@/utils/api";
import { useQuery } from "@tanstack/react-query";
import { useEffect, useState } from "react";

const getDivisions = async (useName: boolean = false) => {
  const { data } = await axiosClient.get<GetDivisionsResponse>(`divisions`, {
    params: {
      fetchCount: "false",
      simpleOnly: "true",
    } as Partial<DivisionSearchParams>,
  });

  return data.rows.map((item) => ({
    id: !useName ? item.id : item.name,
    name: item.name,
  }));
};

interface UseListProps {
  placeholderData?: BasicModel[];
  useName?: boolean;
}

const useDivisionList = (prop?: UseListProps) => {
  //local states
  const [mounted, setMounted] = useState(false);

  const _ = useQuery({
    queryKey: ["division-list"],
    queryFn: () => getDivisions(prop?.useName),
    enabled: mounted,
    placeholderData: prop?.placeholderData,
  });

  useEffect(() => {
    setMounted(true);
  }, []);

  return _;
};

export default useDivisionList;
